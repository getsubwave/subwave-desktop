--- a/src/platform/linux/gtk_host.c
+++ b/src/platform/linux/gtk_host.c
@@ -5209,6 +5209,16 @@
     const char *name;
     int flags;
     native_sdk_secret_schema_attribute_t attributes[32];
+    /* SecretSchema is public ABI despite its reserved tail. Keep the full
+     * layout: libsecret reads these fields even when callers zero them. */
+    int reserved;
+    void *reserved1;
+    void *reserved2;
+    void *reserved3;
+    void *reserved4;
+    void *reserved5;
+    void *reserved6;
+    void *reserved7;
 } native_sdk_secret_schema_t;
 
 typedef gboolean (*native_sdk_secret_password_store_sync_fn)(const native_sdk_secret_schema_t *schema, const char *collection, const char *label, const char *password, GCancellable *cancellable, GError **error, ...);
